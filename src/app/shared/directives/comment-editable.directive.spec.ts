import { CommentEditableDirective } from './comment-editable.directive';

describe('CommentEditableDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentEditableDirective();
    expect(directive).toBeTruthy();
  });
});
